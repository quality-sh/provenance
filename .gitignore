target/
node_modules/
.provenance/cache/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
.env
.env.local

# Dolt database files (added by bd init)
.dolt/

# Copied into packages at pack time from the root LICENSE (single source of truth)
packages/*/LICENSE

# Riptide artifacts (cloud-synced)
.humanlayer/tasks/

# Local Claude Code state (skills are tracked)
.claude/worktrees/
.claude/settings.local.json
