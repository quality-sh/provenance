# Adversarial review: relation shapes cut plan (Fable 5.1)

- Reviewer: Claude Fable 5.1 subagent, Claude Code session https://claude.ai/code/session_01NQ2v24jQRt4G9Yzu2LFMcc
- Date: 2026-09-03
- Reviewed: docs/plans/2026-09-03-relation-shapes-cut.md at d97560a; macro spike reproduced in a scratch crate
- Nothing committed by the reviewer

The report below is the reviewer's final message, reproduced verbatim.

---

Worktree is clean at d97560a; the spike lives in the scratchpad only. Report follows.

# Review of docs/plans/2026-09-03-relation-shapes-cut.md @ d97560a

## VERDICT: REJECT — return for one revision, then re-review

The plan is faithful to the resolution on shape and scope and its counts reproduce exactly (614 rows: 76/19/0/1/0/98/96/1/323; 183 req + 140 res producers; 14/4/3 multi-producer cases; 49 roots; 3 field-only citations; 2 needs-only pairs; 1 `superseded_by`; 0 dangling; all scope `default`). But four defects would each produce wrong code if an implementer executed the plan as written, and two of them (findings 1 and 2) make the plan's own acceptance criteria unmeetable. These are not amendments an implementer can absorb silently; they change sections B, E, G, I and K.

## Findings, most severe first

**1. Traversal direction is undefined after the storage direction inverts; `sdk query impact` and `impact` go blind as specified.** Three of the nine relations invert their stored direction under section C: `references` (source→requirement edge becomes `requirement.source_refs`), `produces` (requirement→rule edge becomes `rule.requirement_ids`), `refines_into`/`spawns` (parent/resolution→child edge becomes child field). Today `queries/impact.rs:34-58` walks `edge.from_id == origin` only and `cache/impact.rs:44-60,81-83` defines Downstream = `to_id`, Upstream = `from_id`. Plan E says `queries/impact.rs` "walks the same way, out-direction only" (plan:225) and W3 item 3 repeats "out direction only" (plan:352). After the cut a Source has no out-relations at all (Source's only field is `supersedes`), and from a Requirement no out-relation reaches a Rule; the walk returns nothing. The harness would catch it, but the plan instructs the wrong walk. Fix: each `RelationDecl` carries a flow orientation (which end is upstream), and impact, trace, traceability and the W3 text derive direction from the declaration, not by hand — which is what the resolution's "the declaration drives every derivation" demands.

**2. The differential harness (I) cannot be green as written, so its "nothing else may differ" is hollow.** Plan:398-405 normalizes only "edge-type names map to relation names" and compares neighbor/prime/traceability triples as sets. (a) Owner side flips for `references`, `refines_into`, `spawns` and `produces` (finding 1), so `(owner, relation, target)` triples differ for all 76+19+1+323 rows. (b) `needs` (98) has no relation name; it must be dropped or folded. (c) After the cut neighbors are "every declared relation in both directions" (plan:266) — so 67 of 68 requirements gain a `domain_id` neighbor, 9 gain question neighbors, sources gain `cites` in-direction rows; today's `walk.rs:64-96` walks edge rows only. (d) The converted contradiction removes the direct requirement↔requirement adjacency and adds a question between them. None of (a)-(d) is in the expected-diff file (plan:401-405). Fix: the normalization is a nine-row table (edge type → relation name, owner flip yes/no, dropped) and the expected-diff file enumerates (c) and (d) by count.

**3. Sequencing K.3/K.4 breaks the "each commit passes the suite" promise; it is the inversion both reviews warned against (gist 06 A9, gist 07 amendment 10).** Commit 3 stops writing edges (plan:417); commit 4 moves the readers and adoption (plan:418-419). Between them `adoption/relationships.rs:59-148` still compares declaration to edge rows and `typed_specs.rs:284-287` still loads edges, so every `sdk apply` adoption test and every write-then-read test (`cli_source_requirement.rs`, `cli_sdk_lifecycle.rs`, wiki tests) is red at commit 3. Fix: either writers write both at 3 and stop at a new commit after 4, or fold 3 and 4 into one commit and say so.

**4. The conversion cannot read v1 state through the store once `SUPPORTED_SCHEMA_VERSION` is 2, and cannot run `check`/`materialize` on v2 output while it is 1.** `readers.rs:143-155` refuses any `version != SUPPORTED` (not only later versions), for every family and the manifest (`state_store.rs:100`). Section G says the converter "rewrite[s] schema_version to 2 … run check; run materialize" (plan:314-316) and K.7 says "constant to 2 … convert the live state" (plan:421-423), while K.3 lands the subcommand with the constant at 1 (plan:417) and the harness runs it against a v1 fixture at K.4. As specified the subcommand either cannot load its input or cannot validate its output. Fix: the converter reads raw JSON below the guard, writes `SUPPORTED_SCHEMA_VERSION` (not the literal 2), and K.3/K.4 use it with the constant still at 1 (writing 1); K.7 flips the constant and re-runs the version rewrite only. Say this in G.

**5. The bare-`StableId` case (required single) is contradictory across B, C, E and I.** B admits `Option<StableId>`, `Vec<StableId>`, `Vec<T> via` only (plan:27-28) and I lists "a `required` single" as a trybuild compile-fail (plan:412). But C keeps `question.topic_id`, `question.requirement_id` (`shaping.rs:146-149`, bare `StableId`) "as today", and E says dangling and `check` become "one generic pass over `declared_relations()`" replacing `add_question_refs` (`dangling.rs:93-139`) and `core.rs:274,297,310,319`. Either bare keys are declared (then "required single" is legal and the trybuild case is wrong) or they are not (then the hand-written question/topic/boundary checks stay and "one generic pass" is false). Also the "5 kinds" concatenation (plan:37) versus Topic/Boundary rows in table C: say whether Topic and Boundary carry the derive. Recommended default: they do (7 kinds), bare `StableId` = required single, trybuild case removed.

**6. `links[]` on question and topic cannot be declared by the attribute as designed.** `ArtifactLink` (`shaping.rs:105-110`) carries a per-entry `target_type`; `#[relation(target = Kind)]` names one kind. `RelationKind::TopicLinks/QuestionLinks` are deleted with the enum (plan:52) with no replacement stated, and `check_artifact_links` (`references.rs:39`) is not mentioned. State that links stay hand-checked and outside the declaration, or add a `target = any` form.

**7. The version bump's blast radius is understated by an order of magnitude.** Plan F names five TS literals (plan:263-265). At d97560a 85 files under `crates/` and `packages/` carry a literal version 1 (`SchemaVersion(1)` or `"schema_version": 1`), including four production writers the plan does not name: `manifest.rs:36` (the `init` default — a v2 binary would refuse the manifest it just wrote), `scope.rs:70`, `threads.rs:60`, `handlers/rules.rs:145`. Risk 9 covers state only. Amend: production literals become `SUPPORTED_SCHEMA_VERSION`; test fixtures enumerated or a grep gate added to the deslop pass.

**8. Section J edits a document that is not in the repository.** No W3/W5 plan exists under `docs/plans/` or `docs/research/` on `origin/main` (only this plan was ever added there: `git log --all --diff-filter=A -- docs/plans`). The referenced text is `docs/research/2026-08-27-qrspi-1wh-query-uniformity-plan.md` on branch `opencode/provenance-20260827T223718Z-87cc1ac4` per the owner's memory notes. The PR cannot apply J as written; name the file and branch, or move the W3/W5 rewrite to the bead.

**9. Risk 8's error message names a command that step 9 deletes** ("a v1 export is refused … with a message naming `convert-edges`", plan:438-439; deletion at plan:426). Also the refusal is not "the schema version guard": `ScopeExport` is `deny_unknown_fields` (`export.rs:8`), so a v1 export dies on the `edges` key with a serde message. Say which and drop the dead reference.

**10. The relation name of the citation is never stated.** C says names use the product word "where the relation has one" (plan:66-67) and leaves the `source_refs` row without one; the wire (`Neighbor.relation`), the harness map, the derived table and `docs/cli.md` all need it. Pick `cites` (already in `RelationKind::RequirementCitesSource`) or `source_refs`, and say it.

**11. `declared_relations()` concatenation test cannot detect an omitted kind.** "Each `RelationOwner` kind appears exactly once" (plan:37-38) catches duplicates only; a forgotten `Question::RELATIONS` passes. The mutation target "delete one row" (plan:410) needs a hand list of expected owner kinds in the test. Name it as such.

**12. Reconcile semantics for the new declaration fields are unspecified.** F adds `refines`, `spawned_by`, `depends_on`, `supersedes`, `resolution_ids` to the typed declaration (plan:255-259); D specifies only `desired_rule` writing `requirement_ids` (plan:158-163). Whether an omitted `refines` in a re-applied spec clears a CLI-set `refines`, or leaves it, decides data loss on every `sdk apply`. Risk 6 covers adoption equality, not reconcile. Default to state: declaration fields are authoritative when present and untouched when absent; and say where that code lands (reconcile.rs is at 476 lines).

**13. The deslop rule "no `edge` word left in production code, comments, or docs" (plan:436-437) is overbroad.** It would rewrite `docs/adr/0003…md:25` (a historical ADR), `lineage_validation.rs:164-165,288` (graph-theory "back edge" in proposal lineage), and `crates/provenance-sdk/PARITY.md`. Exempt ADRs and non-relation uses; and add the doc sites H misses: `docs/shaping.md:233-256` (the `validate_edge_endpoint` example block), `docs/typescript-sdk-poc.md:8`, `docs/state-format.md:119`.

**14. Minor omissions in the reader/deletion list.** `CreateEdgeInput` (`inputs.rs:54-56`), `StateStore::list_edges/closed_edges` (`state_store.rs:167,244`), `graph_records.rs:182-190 load_edges`, `cache/tests/projection_digest_sensitivity.rs:29`, `handlers/schema/artifacts/graph_reference.rs:49,64` (listed in F, fine), the aggregate validator that `materialize` and direct writes run (`docs/cache.md:78-81`) is not named as a place the empty-required-list refusal lives (only `check` and the merge gate, L1).

## Answers

**1. Fidelity.** Position, sentence by sentence: no canonical edge ✓ (A); field or action record ✓; required/optional by type ✓ (C, with L1 deciding enforcement at check/merge/writer, not serde — a defensible reading since serde cannot refuse an empty Vec, and the spike confirms it); reverse lookups derived, projection for served reads, canonical readers from fields ✓ (A, E); one declaration per kind driving writers/validator/gap/projection — ✓ in intent, but findings 1 and 5 show impact direction and bare keys are still hand-wired; rule lists ✓; resolution list + `spawned_by` ✓; requirement refines/depends_on/supersedes ✓; root = no refines ✓ (plan:97-98); source/resolution `supersedes` on newer ✓ with `superseded_by` deleted (plan:88-95; GLM's "no dual form" honored); citation stays ✓; contradicts as question settled by resolution or supersession ✓ (plan:100-104; drops today's shared-resolution settle path at `contradiction.rs:49-57` silently — say it is deliberate); needs dropped with union ✓ (G.5); add+clear per replaced reference ✓ (D, all nine covered); one cut, convert once, bump both versions, delete shard and types, retire the rule, build declaration ✓; raw JSONL read only by code ✓. No compatibility hedge found. Amendments: Fable A1-A5, A7, A8, A10 ✓; A6 superseded by the resolution's question shape ✓; A9 (order) — violated inside the PR (finding 3). GLM 1-6, 8, 9, 11, 12, 13 ✓; 7 ✓ (question); 10 (order) — violated (finding 3); 14 (requiredness per field) — answered implicitly by table C, `MissingSourceRefs` stays a gap (E, frontier "keep their text"). The GLM note that this cut is the first customer of a derivation version is moot here: the byte rewrite to v2 forces a full rederive.

**2. Declaration spike.** Reproduced at `scratchpad/relspike` (syn 2.0.118, quote 1.0.46, proc-macro2 1.0.106, all offline from the lock's versions). Applied to `Requirement` and `Rule` copied verbatim from `artifacts.rs:282-318,404-445` plus the section C fields. Output: `Requirement::RELATIONS` = 6 rows (domain_id, cites/source_refs via source_id, refines, depends_on, supersedes, spawned_by), `Rule::RELATIONS` = 2 rows with `requirement_ids` required; `references()` yields the flat `(name, &StableId)` list; from those one generic function each produced the derived rows, the reverse scan, the dangling report (`"<relation> points at missing <Kind> <id>"`), and the empty-required-list refusal — exactly what B claims. Serde builds an empty `requirement_ids` from a hand-written row without complaint (confirmed). What the derive cannot see: any other struct, so the concatenation is hand-written and a test with a hand list of expected kinds is the only guard (finding 11). What the plan says it cannot do but it can: a `StableId`-typed field on its own struct with no `#[relation]` — a nine-line check in the derive turns it into a compile error (`Resolution.requirement_ids is StableId-typed but carries no #[relation(...)]`, demonstrated). The serde-walking test only names the gap: it sees serialized keys, and every list/option field is `skip_serializing_if` so an unpopulated fixture field is invisible — the guard is only as good as the fixture author. Recommend the compile-time check as primary, the walking test as the guard for references not typed `StableId` (`ArtifactLink.target_id`, `ThreadParent`). A `#[relation(...)]` attribute is invisible to the scanner (`parser.rs:184` matches the key `rule`) — confirmed.

**3. Shapes.** Table C matches the resolution and map rows 1-9, 15, 19, 20 on names, targets, cardinality and requiredness; lists are justified by the 14/4/3 counts; singles by 19/19 and 1. `superseded_by` (source, resolution) → `supersedes` on the newer record, field deleted from both structs and both `--superseded-by` flags (`knowledge.rs`, `policy.rs:42-43` verified). Root = no `refines`, 49/68 ✓. `spawned_by`, `resolution_ids`, `depends_on` present ✓. Missing: the citation relation name (finding 10); the bare-key rows on question/topic/boundary (finding 5); `links` (finding 6).

**4. Commands.** Added/removed/changed list is complete for the nine relations plus the two `supersedes` fields; every mutable reference field has add(or set) and clear; `source-ref clear` fills the map's only eraser gap. `edges create` is not needed after the cut: every path that used it (map rows 2-5, 8) now has a command or a create flag. `requirements refines set` as a subcommand group beside `fog` and `source-ref` (`knowledge.rs:63-72`) is the right home; there is no `requirements update` today and inventing one would be a new hand-made shape. `--target-id` reuse is fine. `questions contradicts set|clear --id` matches `questions answer --id`. Unspecified: cycle refusal on `refines`/`depends_on`/`supersedes` (wiki lineage `traversal.rs:77-124` walks parents; a self- or mutual `refines` needs a guard — edges had none either, so state the default).

**5. Derived table and readers.** Grep of every consumer (list above, 88 Rust files, 3 TS) against sections B, D, E, F, K.9: all production consumers appear with a destination except those in finding 14. Per-scope load is correct: a `relations` row derives from exactly one owner record's fields with no join, so deleting and reloading the owner kind's rows when that family's digest moves keeps catch-up equal to rebuild; `remove_departed_scopes` must delete `relations` explicitly since it is not a family (plan says so). `Unit::Global` shrinks to manifest+dictionary with no derived family — consistent with `docs/cache.md`. The locality guard at `scope_locality_guard.rs:132-147` becomes a scope-only assertion ✓.

**6. Wire.** Verified: `SDK_PROTOCOL_VERSION = 5` (`protocol.rs:25`), `Neighbor.edge_type` (`node.rs:107-113`), `edge_types` on both queries (`query.rs:78,97`), TS `EdgeType` (`protocol.ts:186-195`), `Neighbor`/`NeighborsRequest` (`protocol.ts:246-257`), `index.ts:122`, `SUPPORTED_PROTOCOL_VERSION` (`engine.ts:17`), five TS `schema_version: 1` literals, `GraphExport.edges` under `deny_unknown_fields` (`projection.rs:31,47`), `validate_schema_versions` edge arm (:120), `GraphCounts.edges` (`graph_reference.rs:85,337`), schema artifact (:49,64), `ScopeExport.edges` (`export.rs:27,59-63`), import (`import.rs:40,52`, `scope_writer.rs:52,111-117`). The typed declaration gains exactly the four requirement fields and `resolution_ids` the shapes need; `TypedRuleInput` (`typed_spec.rs:84-88`) requirement optionality contradiction resolved by the engine refusal ✓. Missing: reconcile semantics (finding 12); `TypedDeclarationKind` stays three kinds (no resolution declaration) ✓ stated.

**7. Conversion.** Counts reproduce (614, all per-type). Lossless per type given lists, union for needs, and the superset citation side (0 edge-only) ✓. Refines/spawns "refuse two" is safe (19/19, 1). Idempotence holds only if every list step is a set union and step 9 deletes the field — implied by "sorted and deduplicated", should be explicit for a crash mid-run (publication lock does not make a multi-file rewrite atomic). Ids `topic_contradiction_<from>` / `q_contradiction_<from>_<to>` are deterministic and valid (`StableId::new` checks charset only, `ids.rs:71-77`; no length cap). Already-converted repo: no-op ✓. Records with no relation fields: version rewrite only; the manifest (`state_store.rs:100` applies the guard) and the nested landing records (`landings.jsonl` envelope is unversioned, its two nested records are) are named ✓; `promotion_decisions.jsonl` (76 legacy rows at version 1) is read by the "legacy reader" — confirm it goes through the guard or exempt it explicitly. Mechanism defect: finding 4.

**8. Tests.** Byte-identical is not achievable without the nine-row normalization and an enumerated diff (finding 2). Mutation targets: dropping one `#[relation]` is caught by the compile-time check if adopted, otherwise by the walking test only with a full fixture; skipping an owner kind in the loader is caught by catch-up equivalence ✓; making `requirement_ids` optional ✓; backfilling wrong ✓; deleting a concatenation row needs the hand list (finding 11). Add: a mutation that inverts one relation's flow orientation (finding 1). Untested but required by the resolution: `clear` on the last entry of a required list refuses (stated in D, not in I); the merge gate on `Resolutions`/`Sources` shards (E) has no named test.

**9. W3/W5, sequencing, 500 lines, risks.** Commits: 3→4 is not green (finding 3); K.7's convert step cannot run as ordered (finding 4). File splits are named for writers, CLI, adoption; not for `reconcile.rs` (476) if it gains requirement-field reconcile, nor for `artifacts.rs` (445, gains ~30 attribute lines — within cap). Risks 1-13 each carry an actionable default except 8 (finding 9) and 6/7, which leave reconcile undefined (finding 12). J cannot be applied (finding 8).

**10. Vocabulary.** User-facing: `convert-edges` (dev-only, deleted; acceptable only if no surviving message names it — finding 9); "relation" on the wire (`Neighbor.relation`, `relations` filter) and in `docs/cache.md` — the resolution title uses the word, so it is sanctioned; `--target-id` reused ✓; "root" not exposed ✓; `spawned_by`, `contradicts`, `refines`, `depends_on`, `supersedes`, `requirement_ids`, `resolution_ids` are field names in the resolution ✓. Internal-only names (`RelationDecl`, `RelationOwner`, "deslop", "harness", "dangling") never reach a command or doc. No agent-flavored term found in any proposed command, flag, field, or error text. The gap text `"<relation> points at missing <kind> <id>"` matches today's wording (`dangling.rs:80-85`).

## Checked, clean

- Every file:line cited in the plan that I opened exists at d97560a as described (ce891fe → d97560a differs only in `question.jsonl` and the plan itself).
- `RuleProducer::REQUIRED = [Requirement]` (`graph_query.rs:20`): deleting `OrphanRule` once the list is required loses nothing; 0 orphans today.
- `OpenQuestion` gap (`frontier.rs:94-116`) exists; excluding a `contradicts` question from it while keeping the pair gap avoids a double report; the `explored` topic raises no `UnexploredTopic` gap.
- Wiki, health, prime do not surface questions or topics, so the minted topic/question touch only gaps and neighbors/trace.
- `Manifest`, typed spec, graph reference and the store all check one global constant; "the guard is global" (plan:261) is accurate.
- Merge gate today recognizes Edges, Requirements, Rules, landings only (`validation.rs:34-47,102-113`); E's growth list is right.
- Scanner (`parser.rs:184`) and `provenance-sdk` crate read no edges.
- `syn`, `quote`, `proc-macro2`, `trybuild` are already in `Cargo.lock` at the versions the plan needs; `provenance-macros/Cargo.toml` has no dependencies today.
- `OrphanResolution` deletion is safe: 0 resolutions lack a `resolves` row.
- The two needs-only pairs, three field-only citations, one `superseded_by`, and `res_rule_is_the_function` (status `superseded`, no successor) are exactly as G reports them.


---

# Re-review of revision 2 (ce495d5), same reviewer

# Re-review: revision 2 @ ce495d5

## VERDICT: APPROVE WITH AMENDMENTS (five, all local to I, E, B; none reopens a design point)

All fourteen findings are closed with file:line that checks out at this head. The revision introduces no design defect; it introduces three specification gaps in the harness and one unstated cost in the validator that an implementer would otherwise guess at.

## Findings 1-14: status

| # | Status | Evidence at ce495d5 |
|---|---|---|
| 1 flow orientation | Closed | `flow` on every C row (plan:63-79); directed-walk rule (plan:186-191); `queries/impact.rs` "uses the downstream walk" (plan:221); mutation target "invert one flow" (plan:341). Table checked row by row against today's `impact.rs:81-83` semantics: `cites`, `refines`, `spawned_by`, both rule lists, `resolution.requirement_ids` reproduce today's requirement/source answers. |
| 2 harness normalization | Closed, with residuals (A, B below) | Nine-row table (plan:306-318), owner flips, direction flip for neighbors, enumerated deltas (plan:320-327). |
| 3 commit order | Closed | K.3 double-writes, K.4 readers + adoption + merge gate, K.5 stops edge writes (plan:387-395). |
| 4 converter vs guard | Closed | Reads raw below the guard, writes the constant, `--versions-only`, K.3/K.4 at 1, K.7 at 2 (plan:245-249, 397-400). |
| 5 bare `StableId` / seven kinds | Closed | B: bare = required single (plan:25-26); seven kinds (plan:33); trybuild "required single" removed (plan:346-347). |
| 6 `links` | Closed | Hand-written `links` contribution; `validate_artifact_links` is at `shaping_writers/artifact_links.rs:9`, `check_artifact_links` at `references.rs:39` (plan:38-42). |
| 7 version literals | Closed | Four production sites named (plan:229-230); grep gate with the one exemption, `cli_record_schema_versions.rs` exists. "83 test files" is approximate (93 by my grep including `SchemaVersion(1)` in unit tests); the gate makes the number moot. |
| 8 W3/W5 text location | Closed | File and branch named; edits land on beads 1wh.2/1wh.3 (plan:356-358). |
| 9 dead `convert-edges` message | Closed | `deny_unknown_fields` at `export.rs:8`, serde message, no converter name (plan:216-217). |
| 10 citation name | Closed | `cites` (plan:65, 205). |
| 11 hand-list test | Closed | plan:35-36, 340. |
| 12 reconcile semantics/split | Closed | Present = authoritative, absent = untouched; `desired_requirement` is `reconcile.rs:142-174`; split into `reconcile/{sources,requirements,rules,changes,references}.rs` matches the function groups at `reconcile.rs:16-422` (plan:135-142). |
| 13 deslop exemptions, doc sites | Closed | plan:409-411; `shaping.md:233-256`, `typescript-sdk-poc.md:8` (plan:279-280). |
| 14 omissions | Closed | `CreateEdgeInput` (`inputs.rs:54-61`), `list_edges`/`closed_edges`, `load_edges`, `projection_digest_sensitivity.rs:29`, aggregate validator (plan:174-184, 233-236). |

## Amendments (precise)

**A. Harness (I): trace must be snapshotted with `direction: both`, and the contradiction depth shift listed.** Trace rows are `(node, depth)` (`walk.rs:98-147`), not edge-shaped, so the nine-row table cannot normalize them, and under `out` or `in` every flipped relation changes the reachable set (today `out` from a resolution reaches rules at depth 2 via `produces`; after the cut rules sit in the `in` direction of `requirement_ids`). Under `both`, undirected reachability is preserved (`needs` mirrors `resolves` modulo the 2 union pairs, which the union restores) except that the contradiction pair goes from depth 1 to depth 2 through the minted question. Say: trace and neighbors snapshots use `both`; the expected-diff file lists the pair's depth change and any node whose shortest path ran through that pair.

**B. Expected-diff counts (plan:321-327): three corrections.** (1) Questions with `resolution_id` are 4, not 3, at ce891fe (`git show ce891fe:…/question.jsonl`). (2) Missing: the one boundary with `source_ref` (1 of 3) gains a source neighbor and the source the reverse row. (3) Missing: G.9 writes `res_state_is_jsonl_in_git.supersedes = [res_convex…]`, which today's edge-only walk never showed, so 1 row each way is new. The other counts verify: 67 requirements / 8 domains (all 8 referenced), 9 questions, 5 topics, 3 boundaries, 3 field-only citations.

**C. Aggregate validator (E, plan:233-236; L1): name the added reads.** `validate_ideation_scope_snapshot` (`ideation_batches.rs:143-185`) reads contributions, synthesis packets, proposal cards, assertions, dispositions, legacy dispositions, and landings only; it never loads rules, resolutions, or requirements. "Refuses an empty required list and a cycle" therefore means adding three family reads to that validator (same scope directory, so the locality guard still holds) or adding a sibling graph validator at the same four call sites. State which; it is also where the cycle check for the state-level case (L13) lives.

**D. Compile-time guard (B, plan:31-32): state the exemption for X-class pointers.** `origin_thread` and `origin_message` are `Option<StableId>` on all four graph kinds (`artifacts.rs:266,272,311,317,394,400,438,444`) and point at threads and messages, which the table deliberately leaves out (L12). As written the derive refuses them. Choose a form: a fixed name list inside the derive, or `#[relation(none)]` on the field; the spike used the name list.

**E. Harness scope (I): impact from a resolution changes and is not snapshotted.** With `resolution.requirement_ids` as `target_upstream`, downstream from a resolution no longer contains its requirements (today `resolves` put them downstream). The harness runs impact "per requirement and source" only. Either add resolutions to the impact set and list the delta, or state that resolution-origin impact is out of the parity contract.

## Checked, clean

- Legacy digests: `frozen_digest` serializes the whole record (`legacy_audit.rs:50-60`), so the version rewrite moves both constants (`legacy_audit.rs:19-21`); callers `legacy_validation.rs:23,42` and `ideation_batches.rs:458`; `read_legacy_dispositions` goes through `read_records` and the guard (`readers.rs:206-217`). F's recomputation is required and correctly placed in K.7.
- Converter idempotence: set-union steps plus field deletion in G.9 make a crash rerun a no-op; the version write through the constant closes the K.3/K.7 contradiction.
- K per-commit green claims hold as stated: K.3 adoption still compares edges that writers still write; K.4 moves readers, adoption, and the merge gate together; K.5 stops writes after no reader needs them. K.2's "additive" holds if `superseded_by` stays on the structs until K.3 (its flag is removed there) — implied, not stated; harmless.
- Flow table: `depends_on`/`supersedes` `target_downstream` reproduce today's `from→to` direction (0 rows either way); `domain_id` and the question/topic/boundary keys `none` match today's edge-only impact, which never walked them.
- `links` rows carry a per-entry target kind; the `relations` table's `target_type` column holds it; dangling and check keep the hand-written link checks.
- `check/scope/core.rs:274-332` covers the boundary/topic/question key checks named; the `source_refs` check at :237 folds into the same generic pass and the `superseded_by` checks at :210-218, :344-352 go with the field (not stated; no ambiguity).
- Trybuild set (bad type, unknown key, missing target, undeclared `StableId`) matches what the spike's derive refuses.
- No new invented term reaches a command, field, doc, or message; `flow` values (`target_upstream`, `target_downstream`, `none`) are attribute-internal.
