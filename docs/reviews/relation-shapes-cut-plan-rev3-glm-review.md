# Adversarial review: relation shapes cut plan, revision 3 (GLM)

- Reviewer: GLM adversarial plan review, run `1wh-cut-plan` @ `f2fea1e` (confirmed)
- Reviewed: `docs/plans/2026-09-03-relation-shapes-cut.md` rev 3; resolution `res_relations_are_fields_or_action_records`; gist files 04/06/07; `res_catch_up_hashes_scopes_no_journal`; `docs/cache.md`. Macro spike reproduced from scratch in a scratch crate (syn 2, quote, proc-macro2, offline). Counts recounted from `.provenance/state` at this head. Prior reviews read last.
- Nothing committed by the reviewer except this report.

## VERDICT: APPROVE WITH AMENDMENTS (six; none reopens the design)

The plan is faithful to the resolution, every count reproduces, every amendment from the two draft-position reviews is present or explicitly ruled, and the rev-1 defects are genuinely closed. What remains is one sequencing defect that breaks the per-commit green promise, one product behavior the plan never decides (the rendered "Superseded by" line), and four smaller specification or citation gaps. Each has a one-line fix.

## Findings, most severe first

**1. K.3 cannot be green as scoped: the `superseded_by` struct-field deletion (C, plan:89-91) is a compile error against readers the plan schedules at K.4 or never schedules.** C says "The struct fields and both flags stay until K.3 and go together there." K.3's scope list (plan:393-395) is "writers and commands (section D)... still write the edges; the conversion subcommand; the relation-map test," with the green note "readers and adoption still see the edges." But the deleted field is read by, at minimum: `cache/gaps/dangling.rs:38-72` (both passes), `check/scope/core.rs:210-218` and `:344-352`, `wiki/assemble/pages/source.rs:27-40`, `wiki/assemble/pages/resolution.rs:47-53`, `wiki/model.rs:273,351`, `wiki/render/pages/source.rs:18,73-85`, `wiki/render/pages/resolution.rs:20,136-137`, and the SQL INSERTs at `cache/materialize/graph_records.rs:28-32,146-153` — none named in K.3, and the wiki render layer named nowhere in the plan at all (finding 2). K.4 ("readers move (section E)", plan:397) is where these belong. Amend: delete the fields in K.4 with their readers, or extend K.3's list to name every reader. This is safe either way: the record structs carry no `deny_unknown_fields` (verified in `model/artifacts.rs`, `model/shaping.rs`), so the live v1 `superseded_by` key is silently ignored until the converter rewrites it at K.7.

**2. The rendered "Superseded by" line on source and resolution wiki pages loses its data source, and the plan never decides it.** `wiki/model.rs:273,351` carry `superseded_by: Option<PageLink>`; `wiki/render/pages/source.rs:73-85` and `wiki/render/pages/resolution.rs:136-137` render it as classification rows; the model is fed by `wiki/assemble/pages/source.rs:26-29` and `pages/resolution.rs:47-53`, which C deletes. Section E's reader list and H's doc list name only assemble pages; render/pages and model are absent. The parity harness snapshots the "assembled model" (plan:312-313), not the render, so the silent disappearance of a user-visible line passes the harness. Amend: state the replacement (reverse scan over other sources'/resolutions' `supersedes`, matching the resolution's "supersedes on the newer record"), or record the line's removal as a decision; and say the render layer is out of the parity contract or add it to the snapshots.

**3. The compile-time guard as specified refuses every record kind's own `id` field; the plan states no exemption.** Spike, reproduced: `#[derive(Relations)]` on the post-cut `Requirement` and `Rule` shapes fails with exactly `error: field 'id' holds a StableId but declares no #[relation]; every reference field must carry one (use #[relation(none)] to exempt it)`. B (plan:31-34) states the guard, exempts `origin_thread`/`origin_message` via `#[relation(none)]`, and says "No fixed name list" — leaving the owner `id: StableId` on all seven structs illegal. Amend: state the exemption (`#[relation(none)]` on `id`, a name exemption for `id`, or guard only fields shaped as references by type). Two mechanical notes from the same spike: a shared trait needs `const RELATIONS: &'static [RelationDecl]` — a fixed-length `[RelationDecl; N]` cannot be a trait associated const; and the guard's shape discrimination (bare/`Option`/`Vec<StableId>`, `via` for `Vec<SourceRef>`/`Option<SourceReference>`) otherwise reproduces B exactly: Requirement 6 rows, Rule 2 rows with `requirement_ids` required, `references()` yielding `("cites", &entry.source_id)` for the `via` case.

**4. Wrong citation for the rule-needs-requirement refusal.** F says "the engine refuses a rule with no requirement (`typed_specs.rs:333-358`)" (plan:235). That range is `validate_typed_spec` (schema_version, declared_by, spec, scope — no such refusal). The refusal is `normalize_rule_relationships` (`provenance-core/src/authoring/checks.rs:85-117`, message "must refine at least one requirement" at :97), invoked from `typed_specs.rs:328`. Off by five lines and one function.

**5. F's graph-reference schema surgery misses two `superseded_by` keys.** The artifact edit is specified as dropping `edges` and `edge` (plan:227-228), but the source and resolution closed-record schemas also declare `superseded_by` (`handlers/schema/artifacts/graph_reference.rs:142,197`). Graph reference v2 is minted in the same PR; a v2 schema declaring a field that can no longer exist is stale vocabulary the deslop pass will not catch (it contains no "edge").

**6. H's `docs/cache.md` edit list is incomplete against the paragraph it edits.** It names "the edges shard" (the file says "the edge shards", `docs/cache.md` catch-up ¶1) and "A changed global unit reloads the edges table whole", but the same section also says "Edge rows belong to the global unit and are never deleted on a scope change or a scope departure", and the scope-departure sentence counts "the eighteen scoped tables". After the cut `relations` is a 19th scoped table with no digest row (`res_catch_up_hashes_scopes_no_journal`'s "18 scoped tables" likewise goes stale; E says `remove_departed_scopes` deletes its rows explicitly). The deslop pass catches the first; it will not catch the count.

Minor wording, no amendment required: (a) plan:164 "after the cut a source has no out field" contradicts table C (source `supersedes` is `target_downstream`, i.e. an out field); the claim holds only because 0 source `supersedes` rows exist — reword to "no out relation with rows". (b) plan:162-163 "`none` relations are never followed by impact or traceability and by trace and neighbors only under `direction`" is dense; with `Direction::default() = Both` (`protocol/query.rs:22-27`) neighbors follow none relations by default — one clarifying clause. (c) "83 test files carry a literal 1" (plan:243-244) is pattern-dependent (my greps: 80-95 depending on the literal shapes counted; Fable got 93) — the grep gate, not the count, is the contract.

## Answers 1-10

**1. Fidelity.** Position sentence by sentence: no canonical edge (A) ✓; field or action record ✓ (contradicts becomes the question record, C:79); required/optional by record type ✓ (C table; enforcement at writer/validator/check/merge gate because serde builds empty Vecs — B:39-40, L1); reverse lookups derived, never stored by hand ✓ (A, E; the derived table has no digest row and derives from one owner record with no join, plan:151-152); one declaration per kind driving writers, validator, gap policy, projection ✓ (B; check and the merge gate run generic passes over `declared_relations()`, E:211-213, 201-203; the frontier gaps MissingDomainId/MissingSourceRefs stay hand-written per field but read struct fields, not a hand-maintained vocabulary — the thing the declaration kills is gone with `RelationKind`, `same_fact_as`, `drop_duality_echoes`, B:53-54); rule lists ✓; resolution list + `spawned_by` on the requirement ✓; requirement refines/depends_on/supersedes ✓; root = no refines, 49/68 ✓ (recounted); source/resolution `supersedes` on the newer record with `superseded_by` deleted ✓; citation stays the list on the requirement ✓ (`cites` named); contradicts as a question settled by resolution or supersession ✓, with the shared-resolution settle path dropped on purpose and said so (C:96-98); needs dropped ✓; add and clear per replaced reference ✓ (D, all nine verified below); one cut, convert once, both versions, shard and types deleted, rule retired, declaration in the same cut ✓ (A, F, G, H, K); raw JSONL read only by code ✓ (G: the converter's own line reader). Owner rulings: one cut ✓, no older-binary compatibility ✓ (F: pre-cut references do not verify, "owner accepted"), product words ✓ (checked, below), `depends_on` optional list on the dependent requirement ✓ (C:68, L.4; the question `q_depends_on_owner_or_dropped` answered in state at this head). Draft-review amendments: gist 06 A1-A10 and gist 07's fourteen — present or explicitly deferred where the resolution overrode (the reviews' order amendment was folded into K per rev1 finding 3; the contradiction shape per the resolution). Context follow-ups: anchor restated (H), mechanism chosen before building (B), depends_on decided separately ✓, graph-change view a non-goal ✓.

**2. The declaration (B).** Spike reproduced from scratch (scratch crate, syn 2.0.119/quote/proc-macro2 1.0.107 from the lock): `#[derive(Relations)]` with `#[relation(target, flow, required, name, via, none)]` on the post-cut Requirement (6 rows: domain_id none, cites list `target_upstream` via `source_id`, refines/`spawned_by` single `target_upstream`, depends_on/supersedes lists `target_downstream`) and Rule (2 rows, `requirement_ids` required). Bare `StableId` = required single, `Option` = optional single, `Vec` = list; `via` reads through `SourceRef`; the derive emits the const table and `fn references(&self) -> Vec<(&'static str, &StableId)>`; the undeclared-`StableId` compile error fires (finding 3's wrinkle aside). What a proc-macro cannot see, and whether the guards close it: (i) other structs — cannot; `declared_relations()` is a hand concatenation plus a hand-list test (B:37-38, I:356) — closes duplicates and omissions for the seven kinds, cannot catch a future eighth kind without editing the hand list, which is inherent and acceptable for a fixed product vocabulary; (ii) required-list non-emptiness at deserialize — cannot (serde builds the empty Vec; spike-confirmed) — closed by writer/validator/check/merge-gate enforcement plus the deleted `OrphanRule`/`OrphanResolution` (L1); (iii) per-entry target kinds inside `ArtifactLink` (`shaping.rs:105-110`) — cannot; links stay outside the declaration with hand-written walk, `validate_artifact_links` (`shaping_writers/artifact_links.rs:9`), `check_artifact_links` (`handlers/check/references.rs:39`), and a serde-walking test over a full fixture (B:41-45) — closes the gap as well as a fixture can, given `skip_serializing_if` hides unpopulated fields; (iv) TS types, JSON schema, docs — no guard, hand-written, fine; (v) the scanner — `#[relation(...)]` is invisible to `parser.rs:184` (matches the key `rule`) ✓. `graph_records.rs:39-54` indeed omits `source_refs` (verified), supporting the derive over the const-table alternative. `provenance-macros` has no dependencies today; syn/quote/proc-macro2 are in `Cargo.lock`.

**3. Shapes (C).** Every row checked against the structs and the live state: `domain_id` optional single none (artifacts.rs:302-303) ✓; `source_refs[].source_id` + `clause`, `cites`, optional list, 79 entries/62 requirements/6 without ✓; `refines` optional single on the child, 19 rows/19 distinct children ✓; `depends_on`, `supersedes` optional lists, 0 rows each ✓; `spawned_by` optional single, 1 row (`res_ste_local_dictionary_acquisition` -> `req_ste_local_dictionary_acquisition`) ✓; resolution `requirement_ids` required non-empty, 96 rows over 94 resolutions, `res_dispositions_sole_authority` 3 ✓; rule lists, 183/140, 14 rules >= 2 requirement producers, 4 >= 2 resolution producers ✓; source/resolution `supersedes` on the newer record: the one live value is `res_convex_chosen_as_backend_over_custom_web.superseded_by = res_state_is_jsonl_in_git` -> `res_state_is_jsonl_in_git.supersedes = [res_convex…]` ✓, `res_rule_is_the_function` (status superseded, no successor) reported and left ✓; topic/boundary/question keys unchanged (shaping.rs:117-168) ✓; `contradicts` becomes a question, pair identity (requirement_id, contradicts), settled by `resolution_id` or a `supersedes` list ✓. Cycle refusal on refines/depends_on/supersedes at write, in validator, and in check ✓ (C:61, E, L13).

**4. Commands (D).** All nine replaced references have add/set and clear: refines set|clear, depends-on add|clear, supersedes add|clear (requirement, resolution, source), spawned-by set|clear, source-ref clear beside the existing add (`cli/knowledge.rs:172-188`), rules requirement|resolution add|clear, resolutions requirement|supersedes add|clear, questions contradicts set|clear; create flags `--refines --spawned-by --depends-on --supersedes --contradicts`; `rules create --requirement-id` required repeatable, `--resolution-id` repeatable; `resolutions create --requirement-id` required repeatable. Missing-target and cycle refusals stated; clear on the last entry of a required list refuses with product words. Nothing needs `edges create` after the cut: every act the map lists as edge-only (map rows 2-5, 8) has a command or create flag. `--target-id` is an existing word (`contributions create`, cli/ideation.rs:238) ✓. Removals: `edges create|list|delete` (`cli/graph.rs:5-43`, handlers/edges.rs 57 lines, cli.rs:123-126, handlers/mod.rs:15,117-118), `--superseded-by` (knowledge.rs:28-29, policy.rs:42-43), check's edge pass (handlers/check/edges.rs, 45 lines, check.rs:145) — all verified. `sdk apply`: present-authoritative/absent-untouched rule stated; `source_refs` keeps the append (reconcile.rs:202-208 verified); reconcile split named.

**5. Derived table and readers (E).** I grepped every production consumer of `list_edges`/`closed_edges`/`read_edge_shards`/`EdgeType`/`CreateEdgeInput`/`edges_path`/`add_edge`/`delete_edge` (45 files) and checked each against the plan: every one has a named destination (the full cross-check found no consumer without one; the residual defects are findings 1, 2, 5 above). Migration 021 drops `edges` and its four indexes (002:22-33, 005:1-2) ✓; `ProjectionFamily::Edges` at projection_families.rs:30,53,77,93-96,105-106,140-142 ✓; ALL is 19 rows -> 18 ✓. Catch-up: `rederive_scope` (catch_up.rs:205-225) and `remove_departed_scopes` (148-177) exist as described; `Unit::Global` (181-201) today reloads edges and shrinks to a digest-only arm after the cut, consistent with `docs/cache.md`'s table (no family reads the manifest or dictionary) ✓. Per-scope load and catch-up equivalence hold structurally: a relations row derives from exactly one owner record with no join, so reloading the owner kinds whose family digest moved equals rebuild; the mutation test (skip one owner kind) covers the hand-concatenation risk. Directed walk rule correct and needed: `operations/queries/impact.rs:34-58` follows `from_id` only, `cache/impact.rs:81-83` maps Downstream to `to_id` — after the cut the citation, rule, and resolution inputs sit on the in side, so out-only answers nothing (see nit (a)). `validate_graph_scope` as a sibling of `validate_ideation_scope_snapshot` (`ideation_batches.rs:143-185` reads only ideation families — verified) at the five call sites (`state_store.rs:303`, `materialize.rs:52`, `catch_up.rs:64`, `handlers/export.rs:46`, `handlers/dispositions.rs:80` — all verified) plus check, rejecting the mixed-subject alternative for the right reason.

**6. Wire (F).** `SDK_PROTOCOL_VERSION = 5` (protocol.rs:25) -> 6; `engine.ts:17,35-38` matches. `Neighbor.edge_type` (node.rs:107-113), `edge_types` (query.rs:78,97), `EdgeType`/`Edge` (model/graph.rs:62-99,101-135), `protocol.ts:186-195,246-250,256`, `index.ts:122` — all as cited. Graph reference v2: `GraphExport.edges` (projection.rs:47), `load_projection` `closed_edges` (89), `validate_schema_versions` edge arm (120), `GraphCounts.edges` (graph_reference.rs:85,337), schema artifact (`schema/artifacts/graph_reference.rs:49,64,231-236`), pre-cut references fail (owner accepted) — verified; missing `superseded_by` keys = finding 5. Export/import: `ScopeExport.edges` (export.rs:27,59-63,94-132), import (import.rs:39-42,52; import/scope_writer.rs:52,111-189), v1 export refused on the `edges` key by `deny_unknown_fields` (export.rs:8) with serde's message, no converter name — verified. Typed declarations: `TypedRequirementInput`/`TypedRuleInput` (protocol/typed_spec.rs:64-96) mirror the gains; no resolution declaration, `requirement_ids` CLI-authored — consistent. One global schema constant: `SUPPORTED_SCHEMA_VERSION` (aggregate_validation.rs:19) guards globally and exactly (`readers.rs:85-100,143-155` refuses `version != SUPPORTED`, `state_store.rs:100` for the manifest), hence the converter's raw-JSON reader; four production literals verified (`manifest.rs:36`, `scope.rs:70`, `threads.rs:60`, `handlers/rules.rs:145`) plus five TS literals (`protocol.ts:60`, `spec.ts:326`, `fluent-spec.ts:392`, `bound-materialize.ts:166`, `registry.ts:49`) collapsing into one constant. Legacy digests: `frozen_digest` serializes whole records (`legacy_audit.rs:50-60`), so both constants (`:19-21`) must be recomputed in K.7 with before/after in the PR body — required and correctly placed; the 76 `promotion_decisions.jsonl` rows go through the guard via `read_legacy_dispositions` (`readers.rs:206-217`).

**7. Conversion (G).** Counts recounted at this checkout: 614 rows — 76 references, 19 refines_into, 0 depends_on, 1 contradicts, 0 supersedes, 98 needs, 96 resolves, 1 spawns, 323 produces (183 req + 140 res) — all scope `default`, no `label` key on any row (0 of 614), none dangling. 3 field-only citations, all `src_annotation_format_spec` ✓; 2 needs-only pairs, exactly the two named ✓; 1 `superseded_by` as named ✓. Lossless per type: references is a superset (0 edge-only), so assert-and-add is complete; refines/spawns "refuse two" is safe (19/19 distinct children; 1 spawns); resolves/produces unions reconstruct the lists; needs reports the 2 pairs and unions them into `requirement_ids`. Idempotence: every list step a set union, step 9 deletes the field, rerun on converted state finds no edges directory and no `superseded_by` — no-op ✓; the publication lock does not make a multi-file rewrite atomic, which is why the union/delete design matters (stated). Raw-JSON reading below the guard and writing `SUPPORTED_SCHEMA_VERSION` (never a literal) makes it runnable at K.3/K.4 with the constant at 1 and at K.7 at 2 — the only coherent mechanism given `readers.rs:143-155` refuses both older and newer versions. `--versions-only` at K.7 as the no-op check ✓. Minted ids `topic_contradiction_<from>` / `q_contradiction_<from>_<to>` deterministic; status `explored` avoids `UnexploredTopic`; the question text uses product words; unknown author/date reported. On an already-converted repository: no-op, stated.

**8. Tests (I).** Nine-row normalization table: every row's count verified at this head (76/19/0/1(replaced)/0/98/96/1/183+140); owner flips correct (references, refines_into, spawns, produces); needs dropped. Expected-diff enumerated by count, all verified: 67 requirements with `domain_id` + 8 domains reversed (68 requirements, 67 set, 8 domains); 9 questions gain topic_id+requirement_id, 4 with resolution_id (Direction default is Both, so these appear in the `both` snapshots); 5 topics, 3 boundaries, 1 boundary with `source_ref`; `res_state_is_jsonl_in_git` supersedes 1 row each way (invisible to today's edge-only walk); prime gains the 3 field-only citations; the contradiction pair loses direct adjacency (1 row each way), reaches at depth 2 through the minted question; impact from each of the 94 resolutions loses its requirements at depth 1 (96 rows) because `requirement_ids` is `target_upstream` where `resolves` was downstream — all consistent with `walk.rs:64-96,98-147` walking edge rows only today. Trace and neighbors under direction `both` (A closed); impact per requirement, source, and resolution (E closed). Relation-map test over the gist's 39 rows with per-row authoring and clear commands. Named tests: clear-on-last-entry refusal (rule and resolution), cycle refusals, merge gate on resolutions (empty `requirement_ids`) and sources (duplicate id), validator on empty list and cycle. Mutation targets include inverting one flow, and each maps to a named red test. 49 Rust and 2 TS test files mention edges today — exact match.

**9. Sequencing (K).** Nine commits, seven substantive, each with a stated green story: harness+derive at 1, additive fields at 2, writers double-write at 3, readers+adoption+merge gate at 4, writers stop at 5, projection+wire at 6, constant flip and conversion at 7, records/docs at 8, deletions at 9. The constant stays 1 until 7; the converter writes through the constant. One break: K.3's green story is false because of the `superseded_by` deletion (finding 1) — the only sequencing defect. File splits named and reconcile split into five files matching `reconcile.rs`'s function groups (sources 16-111, requirements 112-219, rules 220-304, changes 305-476, references) ✓; 500-line cap respected with named homes (`AGENTS.md:20`); new test files under 300 lines. Before-ready list (deslop with exemptions, version-literal gate, six mutation runs, clippy, workspace, TS suite, check clean, three reviews) is concrete.

**10. Vocabulary.** No invented or agent-flavored term reaches a command, flag, field, doc, or message. Proposed user-facing surface: field names (`refines`, `depends_on`, `supersedes`, `spawned_by`, `contradicts`, `requirement_ids`, `resolution_ids`, `cites`) are the resolution's own words; `relation` is sanctioned by the resolution's title and position; `--target-id` reused; `--versions-only` and `convert-edges` are dev-only and the subcommand is deleted in K.9 with no surviving message naming it (the v1-export refusal is serde's); the gap text "<relation> points at missing <kind> <id>" matches today's wording (`dangling.rs:76-85`); the minted question text uses product words; `RelationDecl`, `RelationOwner`, "deslop", "harness", "dangling", flow values stay internal. The one user-visible surface the plan leaves undecided is the rendered "Superseded by" row (finding 2).

## Prior-review comparison

Read last, as instructed. Fable rev 1 (REJECT, fourteen findings) and rev 2 (APPROVE WITH FIVE AMENDMENTS), same branch.

**Agree.** All fourteen rev-1 findings are real at rev 1 and genuinely closed at rev 3 — I spot-verified each closure: flow on every C row plus the directed-walk rule and the flow-inversion mutation (1); the nine-row table and enumerated diff (2), with every count reproducing; K reorder (3); raw-JSON converter below the exact-match guard (4); bare = required single, seven kinds, trybuild case fixed (5); links outside the declaration with both check sites named (6); four production literals plus the gate with the `cli_record_schema_versions.rs` exemption (7); J landing on the beads (8); serde's message with no converter name (9); `cites` (10); the hand-list test (11); the reconcile rule and the five-file split matching the function groups (12); the deslop exemptions and both doc sites (13); the deletion list (14). Amendments A-E are all present in rev 3 (direction `both` snapshots and the depth-2 listing; the corrected count 4, the boundary `source_ref` neighbor, the supersedes row; the sibling `validate_graph_scope` at the five call sites; `#[relation(none)]`; impact per resolution).

**Disagree.** Rev 2's "Checked, clean" claims the K green stories hold as stated and calls the `superseded_by` timing residual "implied, not stated; harmless," and separately says the superseded_by checks at `core.rs:210-218,344-352` "go with the field (not stated; no ambiguity)." My finding 1 is exactly that residual, escalated: it is not harmless — the field deletion at K.3 breaks six files whose rewrites the plan schedules at K.4 or never schedules, so the commit-3 green story is false as written. The re-review also under-scoped the blast radius to two check sites; it includes the dangling passes, the graph_records INSERTs, and the wiki assemble+model+render layers.

**Missed by the prior reviews.** (a) The rendered "Superseded by" line and its parity blind spot (finding 2) — rev 1 finding 14's omission sweep and rev 2's amendment pass both stopped at the assemble pages. (b) The owner-`id` compile-guard wrinkle (finding 3) — both reviews reproduced the spike and both applied it to structs copied verbatim with `id: StableId` present; neither reported that the guard as specified refuses every record's own id, and rev 3's "no fixed name list" makes the exemption unstateable as written. (c) The schema artifact's `superseded_by` keys (finding 5). (d) The `docs/cache.md` sentence set, including the "eighteen scoped tables" count going stale against `res_catch_up_hashes_scopes_no_journal` (finding 6). (e) The `typed_specs.rs:333-358` mis-citation (finding 4) — rev 1 accepted the pointer without opening it.

## Checked, clean

- Every count in C, G, and I recounted at `f2fea1e` and reproduced exactly (614 rows by type; 183/140; 14/4; 49/68 roots; 67 domain_id; 79/62/6 citations; 3 field-only; 2 needs-only; 1 spawns; 1 superseded_by; 94 resolutions; 96 resolves; 0 label; all scope default; 5 topics/3 boundaries/1 source_ref/9 questions/4 settled/8 domains; 76 legacy rows; 49+2 test files; 19->18 families).
- Every file:line citation I opened exists as described, with two exceptions (finding 4; the "edges shard"/"edge shards" quote in finding 6). That includes all of B, C, D, E, F, G's named sites: relations.rs:26-29,33-40,49-71,215-239; front.rs:17-92,94-118,121-131,338-372; parser.rs:184; artifacts.rs and shaping.rs as cited; inputs.rs:23,54-61,105,126-127,317; state_store.rs:100,167-169,244-246,303; readers.rs:85-100,143-155,206-217,357-428; writers.rs:11-55,131-187,189-320; rule_writers.rs:10-87,93-174; graph_query.rs, frontier.rs, contradiction.rs, dangling.rs, state_adapter.rs, prime.rs, impact.rs (queries and cache), traceability.rs, health.rs, walk.rs, plan.rs:131-139, requirement_reviews.rs:132-149 (called at typed_specs.rs:252); reconcile.rs 476 lines with the cited ranges; relationships.rs:8-37,68-145; adoption/relationships.rs:59-148; typed_specs.rs:33,284-287; ideation_batches.rs:143-185; merge/validation.rs:36,62-63,103,182-194; graph_records.rs as cited; family_rows.rs:43-45,87; projection_digest.rs:17-18; scope_locality_guard.rs:132-147 and projection_digest_sensitivity.rs:29 (both under cache/tests/); projection.rs and graph_reference.rs as cited; export/import as cited; protocol.rs:25; model/graph.rs:62-135; node.rs, query.rs; manifest.rs:36, scope.rs:70, threads.rs:60, handlers/rules.rs:145; legacy_audit.rs:19-21,50-60; cli.rs, cli/graph.rs, cli/knowledge.rs, cli/policy.rs, handlers/mod.rs, handlers/{edges,check/edges,requirements,rules,resolutions,export,import,dispositions}.rs; check/scope/core.rs:274-332; artifact_links.rs:9; references.rs:39; migrations 002/005; the TS files and both spec tests; wiki sites; docs lines in H (state-format.md 120 lines, cli.md 471 lines, shaping.md:63,71,233-256, typescript-sdk-poc.md:8, cache.md paragraphs).
- `docs/cache.md`'s derivation table confirms no family reads the manifest or dictionary, so the `Unit::Global` arm shrinking to a digest update is sound.
- `Direction::default() = Both` (query.rs:22-27) makes the `both` snapshots the default-shape contract.
- No `deny_unknown_fields` on the record structs, so the live `superseded_by` key is inert between the field deletion and the K.7 rewrite.
- `jsonl.rs` atomic writer, `compile_fail.rs` pattern, `grf1_`/`git1_` formats, `coverage/retired.rs`, `Cargo.lock` syn/quote/proc-macro2, `provenance-macros` dependency-free: all as the plan assumes.
- `RelationSource`, `related_nodes`, `RelationDirection`, `RelationEndpoint` survive as E requires; `RecordFront.edges` and the per-kind arms are where B says.
- The scanner reads markers, not attributes (`parser.rs:184`); `provenance-scanner` touches no edges.
